# Assignment 2

## Kth largest element in an array
Find the kth largest element in an array. Should be O(n) time complexity

## Smallest sub array with sum greater than target
Find the smallest sub array with a sum greater than the target. 

## Majority element
Find the majority element in an array.

## Remove deuplicate letters lexigraphically
Remove duplicate letters from a string, such that every letter appears once and only once. The result should be the smallest lexigraphical string possible. 

## Maximum gap
Find the maximum gap between two elements in an array, the array is unsorted. Should be O(n) time complexity